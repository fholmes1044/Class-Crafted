<!DOCTYPE html>
<html>
  <head>
    <title>Classcrafted</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    
    <%# <%# add link_to here with conditional handling% if logged in what should they see> %>
    <div class="navbar">
      <nav>
        <%= link_to "signup", signup_path%>
        <%= link_to "login", login_path%>
      </nav>
    </div>
    <%= yield %>
   <%# build class for navbar %> 
  </body>

  
</html>
